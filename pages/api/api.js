// api.js

const express = require('express');
const router = express.Router();

// Define your server-side routes and middleware here

module.exports = router;

// add update Balance route
